{
  "buildCommand": "cd ng && npm ci && npm run build",
  "outputDirectory": "ng/dist/ng/browser",
  "installCommand": "cd ng && npm ci",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
